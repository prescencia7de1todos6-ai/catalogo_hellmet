name: Mantener Supabase activo

on:
  schedule:
    - cron: "0 12 */3 * *"   # cada 3 días, a las 12:00 UTC
  workflow_dispatch:          # también lo puedes correr manual desde GitHub

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Consulta simple a Supabase
        run: |
          curl "https://zdfljhgxoyccsyljhqri.supabase.co/rest/v1/categoria?select=id_cate&limit=1" \
            -H "apikey: sb_publishable_fshkZejvNfN1qQEJOw9-Hg_OaAhG_rf" \
            -H "Authorization: Bearer sb_publishable_fshkZejvNfN1qQEJOw9-Hg_OaAhG_rf"
